const SUPABASE_URL="https://ajxcdgneroabkgoufjor.supabase.co",SUPABASE_KEY="sb_publishable_KGN1SeLryFEg9n9iaoxAag_s2OuqeFJ",db=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY);
const BASE_ITEMS=["垃圾箱","鏡面","皂液瓶","洗手盆","地面","牆身","天花風槽","尿兜","坐廁","廁紙架／抹手紙架"],$=id=>document.getElementById(id),form=$("inspectionForm"),checklist=$("checklist"),template=$("checkTemplate");let results={};
function items(){return $("toiletType").value==="傷健廁"?BASE_ITEMS.filter(x=>x!=="尿兜"):BASE_ITEMS}
function renderChecklist(){const valid=new Set(items());Object.keys(results).forEach(k=>{if(!valid.has(k))delete results[k]});checklist.replaceChildren();items().forEach(name=>{const card=template.content.firstElementChild.cloneNode(true);card.querySelector("span").textContent=name;card.querySelectorAll("button").forEach(button=>{button.classList.toggle("selected",results[name]===button.dataset.value);button.onclick=()=>{results[name]=button.dataset.value;renderChecklist()}});checklist.append(card)});$("progress").textContent=`${Object.keys(results).length} / ${items().length}`}
function setMessage(text,success=false){$("formMessage").textContent=text;$("formMessage").classList.toggle("success",success)}function setBusy(busy){form.classList.toggle("is-busy",busy);$("submitBtn").disabled=busy}function setCloud(text,error=false){$("cloudState").textContent=text;$("cloudState").parentElement.classList.toggle("error",error)}
$("building").onchange=()=>{const is3E=$("building").value==="3E";$("coreLabel").hidden=is3E;$("core").required=!is3E;if(is3E)$("core").value=""};$("toiletType").onchange=renderChecklist;$("markAllPass").onclick=()=>{items().forEach(x=>results[x]="pass");renderChecklist()};$("refresh").onclick=loadRecords;
form.onsubmit=async event=>{event.preventDefault();setMessage("");if(!form.reportValidity())return;const current=items();if(current.some(x=>!results[x])){setMessage("請為每個清潔項目選擇合格或不合格。");return}setBusy(true);setCloud("正在保存…");const record={building:$("building").value,core:$("building").value==="3E"?null:$("core").value,floor:$("floor").value,toilet_type:$("toiletType").value,checks:results,notes:$("notes").value.trim()};const {error}=await db.from("toilet_inspections").insert(record);setBusy(false);if(error){setCloud("保存失敗",true);setMessage(`提交失敗：${error.message}`);return}setCloud("已保存雲端");setMessage("巡查記錄已提交。",true);results={};$("notes").value="";renderChecklist();await loadRecords()};
async function loadRecords(){setCloud("正在同步…");const {data,error}=await db.from("toilet_inspections").select("*").order("created_at",{ascending:false}).limit(50);if(error){setCloud("同步失敗",true);$("recordList").replaceChildren();$("empty").hidden=false;$("empty").textContent=`讀取失敗：${error.message}`;return}setCloud("雲端記錄");const list=$("recordList");list.replaceChildren();$("empty").hidden=data.length>0;$("empty").textContent="暫時未有巡查記錄。";data.forEach(record=>{const checks=record.checks||{},failed=Object.entries(checks).filter(([,v])=>v==="fail").map(([k])=>k),total=Object.keys(checks).length,passed=total-failed.length,article=document.createElement("article");article.className="record";article.innerHTML='<span class="date"></span><div><strong></strong><div class="badges"></div></div><span class="score"></span>';article.querySelector(".date").textContent=new Date(record.created_at).toLocaleString("zh-HK",{dateStyle:"medium",timeStyle:"short"});article.querySelector("strong").textContent=`${record.building}${record.core?` · ${record.core}`:""} · ${record.floor} · ${record.toilet_type}`;const badges=article.querySelector(".badges");(failed.length?failed:["全部合格"]).forEach(x=>{const badge=document.createElement("span");badge.className="badge";badge.textContent=failed.length?`${x} ✕`:x;badges.append(badge)});const score=article.querySelector(".score");score.textContent=`${passed}/${total}`;score.classList.toggle("has-fail",!!failed.length);if(record.notes){const note=document.createElement("p");note.className="record-note";note.textContent=`備注：${record.notes}`;article.append(note)}list.append(article)})}
renderChecklist();loadRecords();
